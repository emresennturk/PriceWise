spring.application.name=PriceWise
# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/pricewise_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=emberes1617.


# Hibernate Settings (JPA)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Firebase configuration (path to the JSON key file)
firebase.config.path=classpath:firebase-adminsdk.json

# Email configuration for JavaMail (using Gmail SMTP as an example)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=benemresenturk@gmail.com
spring.mail.password=emberes1617
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true